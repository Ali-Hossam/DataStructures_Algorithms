#pragma once

// returns the sum of an array using recursion technique
int sumArray(int* arr, int size, int i=0);

// returns number of elements in an array using recursion technique
int countArray(int* arr, int tail, int i=0);

// returns the maximum number in an array using recursion technique
int maxArray(int* arr, int size, int i = 0, int max = 0);